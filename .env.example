# Sem query params na URL: o driver postgres.js repassa cada um como
# parâmetro de conexão do Postgres, então algo como ?schema=public
# derruba a conexão com "unrecognized configuration parameter".
# Credenciais e porta abaixo batem com o docker-compose.yml deste repo.
DATABASE_URL="postgres://postgres:postgres@localhost:12345/app"
