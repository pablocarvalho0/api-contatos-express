{
  "name": "api-contatos-express",
  "version": "1.0.0",
  "description": "API REST de contatos em Express + TypeScript — projeto de estudo focado no ciclo de vida da requisição, escopo de middlewares e tratamento centralizado de erros.",
  "main": "index.js",
  "scripts": {
    "dev": "node --env-file=.env --import tsx --watch src/server.ts",
    "build": "tsc",
    "test": "bash src/tests/test-rotas.sh",
    "db:pull": "drizzle-kit introspect && mv -f src/db/drizzle/schema.ts src/db/drizzle/relations.ts src/db/",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "keywords": [],
  "author": "Pablo Carvalho <pablocnsantos@gmail.com>",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "express": "^5.2.1",
    "postgres": "^3.4.9",
    "typescript": "^7.0.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.1",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.23.5"
  }
}
